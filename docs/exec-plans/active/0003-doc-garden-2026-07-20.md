# 0003 — doc-garden 2026-07-20

## 1. Purpose

Routine doc-gardener scan surfaced 4 categories of actionable failures across 11 checks. The most consequential are: PRD-to-spine drift (35 issues — 15 hash mismatches + 20 untracked files, a recurrence since the 2026-07-04 garden), QUALITY_SCORE.md regrade overdue (was due 2026-07-18), and 26 `verification-status: proposed` files stale again 16 days after the last date bump. Also: 6 docs (5 conventions + PLANS.md + the doc-gardener skill itself) are at 80 days since last review and will cross the 90-day threshold in 10 days.

## 2. Big Picture

All fixes are documentation-only — no `src/` changes, no enforcement-layer changes. The PRD drift (M1) is recurrence of the 0002 M5 fix — the PRD files have evolved since the last sync. The stale proposed files (M2) are the same stubs for non-Argus apps that were date-bumped in 0002; the honest action is to bump them again while noting the pattern.

Deliberately out of scope: DOM-level verification of `implemented` status (no rendered surfaces to check); content refresh of stale `proposed` files beyond date bumps (they remain stubs for non-Argus apps — see 0002 §6); archiving Metis/Hermes specs (that's a product decision, not a gardener task — the 0002 retrospective flagged it as "what to watch").

## 3. Milestones

### M1 — Resolve PRD-to-spine drift with harness-go-sync

35 drift conditions detected:
- **15 HASH_MISMATCH**: Every tracked PRD file has modified content since the 2026-07-04 manifest regeneration.
- **20 UNTRACKED_FILE**: Includes 1 stale `.bak` file, 2 image files (`.png`, `.jpeg`), 1 excalidraw file, 1 HTML spec, and 15 eval/ rubric and skill files not yet tracked.

Run `harness-go-sync` to regenerate the manifest from current PRD state. The affected stages per `stage_affinity` are `01-product-sense`, `02-design`, and `03-architecture`.

`Acceptance Test:` `uv run pytest .claude/tests/test_prd_spine_drift.py -v` — all test cases pass (symlink resolves, manifest valid, no hash mismatches, no new files, no orphans).

### M2 — Update last-reviewed dates on 26 stale verification-status: proposed files

The 26 files flagged have `verification-status: proposed` unchanged since the 2026-07-04 date bump. They are the same stubs for non-Argus apps (Metis, Hermes, shared pipelines) that 0002 documented. Update `last-reviewed` to `2026-07-20`.

Three design-docs flagged: `business-diagnosis.md`, `qa-scoring.md`, `autonomous-service-agent.md`.
Twenty-three product-specs and remaining design-docs (same list as 0002 M3, minus any that were since updated).

`Acceptance Test:` walk all files under `docs/` with `verification-status: proposed` in frontmatter; assert `last-reviewed:` date is within 14 days of today (2026-07-20).

### M3 — Regrade QUALITY_SCORE.md

`Next regrade: 2026-07-18` is 2 days past. No active exec-plan covers the regrade. Run the grader subagent to produce a fresh matrix. If the grader subagent is unavailable, bump the `Next regrade` date to 2026-08-03 (14 days) and add a Decision Log entry explaining the deferral.

`Acceptance Test:` QUALITY_SCORE.md `Next regrade` date is in the future, OR the matrix has been freshly graded with evidence for every cell.

### M4 — Refresh last-reviewed dates on 6 approaching-stale docs

Six files were last reviewed 2026-05-01 (80 days ago) and will cross the 90-day threshold on 2026-07-30:
1. `docs/conventions/ask-threshold.md`
2. `docs/conventions/i-dont-know-protocol.md`
3. `docs/conventions/deps-and-secrets.md`
4. `docs/conventions/commit-hygiene.md`
5. `docs/conventions/layering.md`
6. `docs/PLANS.md`

Also: the doc-gardener skill itself (`.claude/skills/doc-gardener/`) was last reviewed 2026-05-01 (80 days).

Review each file for factual accuracy; update `Last reviewed:` to `2026-07-20`. If any file's content is stale beyond the date, add a Decision Log entry instead of silently bumping the date.

`Acceptance Test:` walk each of the 6 convention docs + PLANS.md + the doc-gardener skill; assert `Last reviewed:` date is `2026-07-20`.

### M5 — Add product-spec reference to core-beliefs.md

`docs/design-docs/core-beliefs.md` does not link to any product-spec. This was excluded from 0002 M4 Part B ("excluding core-beliefs.md and index.md"). Since core-beliefs.md is a design-doc, it should reference at least one product-spec for traceability. The most relevant product-spec is `docs/product-specs/index.md` as core beliefs span all products.

`Acceptance Test:` `docs/design-docs/core-beliefs.md` contains a link to a file under `docs/product-specs/`.

## 4. Progress

- [x] M1: Resolve PRD-to-spine drift with harness-go-sync
- [x] M2: Update last-reviewed dates on 26 stale verification-status: proposed files
- [x] M3: Regrade QUALITY_SCORE.md
- [x] M4: Refresh last-reviewed dates on 6 approaching-stale docs
- [x] M5: Add product-spec reference to core-beliefs.md

## 5. Decision Log

### Decision: create new garden plan rather than extending 0002

**Rationale:** `Source: docs/conventions/i-dont-know-protocol.md` — 0002 is fully completed (all 6 milestones checked off, outcomes documented). Extending a completed plan would violate the ExecPlan lifecycle in PLANS.md. A new plan with a fresh scan is the correct action. The recurrence of PRD drift and stale proposed files is a pattern worth noting but not a failure of 0002 — those fixes were correct at the time; the files simply evolved again.

### Decision: bump stale proposed dates again rather than archive

**Rationale:** `Source: 0002 §6 Surprises & Discoveries` — the 25+ `proposed` files are stubs for non-Argus apps. Archiving them is a product decision (the 0002 retrospective explicitly flagged this as "what to watch"). Until the human decides to archive or flesh them out, the gardener's job is to record that they were reviewed and remain stubs. This is the second consecutive bump (0002 M3 → 0003 M2); if this pattern continues, the next garden should flag it as a process issue.

## 6. Surprises & Discoveries

### The PRD drift recurred within 16 days of the last sync

0002 M5 regenerated the manifest on 2026-07-04. As of 2026-07-20, all 15 tracked files show hash mismatches. This is not a regression — the PRD files are actively being edited (most recent commits touch PRD content). The frequency suggests the manifest should be regenerated more often, or the PRD_MANIFEST should be treated as a living artifact rather than a snapshot.

### The doc-gardener skill itself is approaching staleness

The skill file was last reviewed 2026-05-01 (80 days). The skill's own `Last reviewed` footer is the mechanism; the gardener eating its own dog food is correct behavior.

## 7. Awaiting Steering

(empty — all milestones are documentation-only fixes within established conventions. No Tier C decisions.)

## 8. Clean Checks (no action needed)

| Check | Result |
|---|---|
| Citation domain quality | Clean — no aggregator citations (medium.com, dev.to, etc.) |
| dep-override audit | Clean — no `[dep-override-approved-by-human]` commits |
| Forbidden hand-edits in `docs/generated/` | Clean — `docs/generated/` directory does not exist |
| Past-deadline Confidence:low dates | Clean — all date-based Revisit: deadlines are in the future; milestone-based ones (M3, "when escape-rate data accumulates") remain gated on future work |
| Previous garden fixes | Verified — M1 broken links in architecture-ref.md and harness-engineering-llm.md remain fixed; no regression |
