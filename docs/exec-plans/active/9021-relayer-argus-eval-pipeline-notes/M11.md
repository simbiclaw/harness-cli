# M11 — Re-litigate NEI scoring

Implemented alongside M10, inline by the main session — the same harness
violation recorded in `M10.md`. Adversarial verification by subagent B: **in
flight.** No checkbox flip until a CONFIRMED verdict is recorded.

### [plan-confirmed] 2026-09-13 — Upstream really does ship half a point for an unverifiable row

`simbiclaw/sim@0c2cccd core/fact_checker.py:184-185` scores NEI at 0.5, and
`core/aggregator.py:45-50` keeps it in `applicable`, so it lands in the
denominator. A criterion nobody could check contributes half a point to a
shipped number.

### [deviation] 2026-09-13 — `HUMAN_REVIEW` has the same defect and the plan did not mention it

- **What the plan said:** NEI only.
- **What the code revealed:** Path C (`core/fact_checker.py:124-136`) returns
  `HUMAN_REVIEW` with `score=0.5` and `requires_human_review=True` — the flag
  sits *beside* a score that still counts. Same failure, different enum member.
- **Conservative choice:** Both leave the numerator and the denominator and are
  recorded as deferrals. `NA` keeps its upstream treatment — excluded from the
  arithmetic but **not** a coverage gap, since conflating the two would defer
  every call that skips an optional row.
- **Revisit:** M19 maps these to §6's three `defer_reason` values.

### [discovery] 2026-09-13 — This changes every score upstream has produced

Intended, not a regression. A three-row call with two passes and one NEI
reports 83.3 upstream and 100.0 here; the acceptance test uses exactly that
case, because a one-pass-one-fail-one-NEI case gives 50.0 under both policies
and would have proved nothing.

### [human-todo] 2026-09-13 — `auto_final_eligible` deliberately cannot be answered from one axis

D10 says neither gate substitutes for the other, so `RawScore` exposes
`coverage_clear` — the coverage axis only — and the module-level
`auto_final_eligible()` takes `criteria_trusted` keyword-only with no default.
A caller who has not measured κ must pass `False` explicitly rather than
receive an auto-final answer by omission.
