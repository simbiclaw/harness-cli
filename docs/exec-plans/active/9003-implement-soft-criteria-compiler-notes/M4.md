# M4 — Agreement seeder + deduction setter (9003)

### [plan-confirmed] — Four pure functions landed per contract

seed_agreement_gate (A5: tau 0.8, both tails seeded, current_kappa null),
set_deduction_weight (A6: finite-real or 1.0, never scaled by corroboration),
set_w_c (A6: 0.4 PROVISIONAL shared constant, patch-1 D6), set_iteration_policy
(A7: write-time epoch commit only). Purity: stdlib (math) only — I1 intact.

### [discovery] — Numeric robustness of deduction weights

B-verification found: out-of-float-range ints crash float() with OverflowError;
NaN/Inf pass the isinstance gate despite the "real number" contract. Closed
with math.isfinite + try/except (OverflowError, ValueError, TypeError) →
1.0. Finite extremes (1e308, subnormals) pass through unchanged.

### [plan-confirmed] — Seeded blocks are AUTH-3/6-compliant by construction

Both tails always seeded; check_agreement_gate + check_escape_plan return []
on every seeded shape (incl. garbage criteria). The missing-escape-plan
rejection is exercised via the M1 validator directly.
