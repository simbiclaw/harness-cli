# M5 — Binary→continuous bridge (9003)

### [plan-confirmed] — Five pure functions landed per contract

bind_item_to_dimension (B-A), compile_applicability_gate (B-B),
synthesize_hard_fail (B-C, many-to-one, no copied threshold),
extract_values (B-D, confidence 1.0), check_dimension_coverage
(Item-24 pattern → coverage-gap row + defer). Purity: stdlib +
agreement only (deduction via set_deduction_weight, finite-real guard).

### [plan-confirmed] — AUTH-9 gate in the binding

manifest_epoch truthiness (stripped) decides severity_map + auto_final:
empty/whitespace epoch degrades to the no-manifest behavior (auto_final
False, no ref). Caller-side (M7 loader) owns epoch existence checks —
verifying a manifest exists would be I/O, quarantined out of core (I1).

### [discovery] — Value extraction never fabricates

Keyless threshold entries (missing name/threshold, string thresholds,
NaN/Inf, huge ints) are skipped — no threshold signal at confidence 1.0
from invented values. Hard-fail triggers dedupe item ids.

### [discovery] — Runner drift risk (M6a)

The skill runner (run_compile.py) still inlines its own bind/severity/
gap-row logic instead of importing M1-M5 — already diverged (hardcoded
epoch-000, coverage row without data_dependency). M6a must rewire the
runner onto the pure core per round-3 decision 1 ("the deterministic
functions of M1-M5 are the loop's backbone").
