# M7 — Calibration manifest channel (9003)

### [plan-confirmed] — Independent channel per round-3 Q8

load_manifest (io) + apply_manifest_epoch (pure) + `manifest inject` runner
subcommand. Manifest at `_meta/calibration-manifest.<epoch_id>.yaml` (only
the exact aligned name loads); source_case validated STRUCTURALLY
(cookbook.<slug>.yaml / errors.<slug>.yaml grammar) — never existence-checked
(the libraries are empty); epoch_id strict M0 regex (40-hex).

### [plan-confirmed] — AUTH-9 re-evaluated in BOTH directions

apply_manifest_epoch computes auto_final_allowed = (criterion in covered) for
surface-form nodes — grants on coverage, REVOKES on regression to a
non-covering epoch. Only severity_map + auto_final_allowed ever change;
signals/corroborators/agreement byte-identical (no-recompile holds).

### [discovery] — affected_criterion bare-id convention

The loader enforces ^\d+$ on affected_criterion — the prefixed form "C21"
would silently never grant (rejected with a convention-naming error).
"021" (padded) is accepted-but-ineffective: structural-only by contract,
3-digit ids are legitimate for larger rubrics.

### [discovery] — Test-constant discipline

EPOCH_2 was mis-written twice (39-hex then 38-hex) before landing as a
verified 40-hex run — the strict M0 regex caught the loader bypass (a
model_construct workaround that had to be reverted) before it shipped.
PYEOF
python3 - << 'PYEOF'
p = "docs/exec-plans/active/9003-implement-soft-criteria-compiler.md"
text = open(p).read()

old = "- [ ] M7: Calibration manifest channel (independent)  (created 2026-07-08)"
new = "- [x] M7: Calibration manifest channel (independent) — flipped 2026-08-12  (created 2026-07-08)"
assert old in text
text = text.replace(old, new, 1)

anchor = "## 6. Surprises & Discoveries"
entry = """### M7 adversarial verification (2026-08-12)

Verdict: CONFIRMED (round 2)

**Rationale:** `Source: subagent B adversarial verification, rounds 1-2 (2026-08-12)` — acceptance tests 11/11 manifest + 191 milestone + 195 structural; ruff clean. B's REJECTED verdict closed with a red test + fix round: AUTH-9 re-evaluated in BOTH directions (an epoch regression to a non-covering manifest now REVOKES auto-final — the F1 stale-grant block), file-name epoch alignment made unconditional (only `calibration-manifest.<epoch_id>.yaml` loads — F2), `affected_criterion` bare-id convention enforced (F3). Round 2 CONFIRMED — 98/98 adversarial checks, zero findings. The loader is structural-only per round-3 Q8 (source_case grammar, never existence-checked — the libraries remain empty). Verified at 23275b6.

""" + anchor
assert anchor in text
text = text.replace(anchor, entry, 1)

old_s = "* 2026-08-12 (M6a): REPO_ROOT off-by-one (parents[3] resolved to .claude/) silently broke the default fixture path and sys.path fallback — masked by the editable install; closed at parents[4]."
new_s = old_s + """
* 2026-08-12 (M7): AUTH-9 is now fully bidirectional — the M7 injection channel can both grant and revoke auto-final; the M1 check_calibration_coverage remains the validator-side gate, the inject channel the authoring-side re-evaluation.
* 2026-08-12 (M7): a test-constant typo (39-hex, then 38-hex epoch) briefly motivated a loader workaround (model_construct bypassing the M0 pattern) — the strict M0 regex caught it before shipping; the constant was corrected to a verified 40-hex run and the bypass reverted."""
assert old_s in text
text = text.replace(old_s, new_s, 1)

open(p, "w").write(text)
print("plan updated")
PYEOF
git add docs/exec-plans/active/9003-implement-soft-criteria-compiler.md docs/exec-plans/active/9003-implement-soft-criteria-compiler-notes/M7.md
uv run pytest .claude/tests/ -q 2>&1 | tail -1
git commit -q -m "flip(m7): M7 calibration manifest channel CONFIRMED — 2 adversarial rounds

Plan: docs/exec-plans/active/9003-implement-soft-criteria-compiler.md#milestone-7
Decision: subagent B CONFIRMED (round 2, 98/98 checks); AUTH-9 bidirectional; verified at 23275b6"
git log --oneline -1
