# M2 — Declare PyYAML as a direct dependency (9009)

### [plan-confirmed] — dep-vetter APPROVED, declared, lock synced

pyyaml vetted via the four-check policy: Age 5526 days, Downloads
292,078,155/week, Activity 61 days (2026-06-17 commit), License MIT — all
PASS. Declared `pyyaml>=6.0` in pyproject.toml (Tier C, steering granted),
uv.lock synced, test_dep_vet_content + befine + pipeline + full structural
suite green. Resolves the regrade's io boring-tech-D finding.
