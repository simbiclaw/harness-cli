# 9007 — Doc Garden 2026-07-31

## 1. Purpose

Routine doc-gardener sweep. Fix broken cross-references and regrade overdue QUALITY_SCORE.

## 2. Big Picture

Two categories of failures:

1. **QUALITY_SCORE regrade overdue.** `Next regrade: 2026-07-18` is 13 days past. No active exec-plan covers the regrade. Need to run the grader subagent and update scores, or push the deadline forward with a reason.

2. **10 broken relative links.** Most are path resolution bugs (`docs/docs/...` double-prefix, or relative paths from nested directories that don't resolve correctly). Fix or remove the broken links.

No other checks failed: no stale docs (>90 days), no PRD drift, no dep-overrides, no forbidden hand-edits, no generated directory exists.

## 3. Milestones

### M0 — Regrade QUALITY_SCORE.md

Run the grader subagent against the current state of the repository. Update `QUALITY_SCORE.md` with new grades and set `Next regrade: 2026-08-14` (14 days from now).

`Acceptance Test:` `.claude/tests/test_quality_score_regrade.py` — QUALITY_SCORE.md has `Last graded` within the last 7 days and `Next regrade` in the future.

`Behavioral Test: none` — pure documentation update; quality regrade has no functional behavior to exercise.

**File Scope:**
- `Allowed Reads:` docs/
- `Allowed Writes:` docs/QUALITY_SCORE.md
- `Requires:` none
- `Risk Tier:` B

### M1 — Fix broken cross-references

Fix the 10 broken relative links. Root causes:
- Design docs use `../docs/product-specs/` (double `docs/` prefix) — should be `../product-specs/`
- Completed plan 0002 references local files (`./guide.md`, `./syntax.md`, `architecture.md`) that don't exist
- `harness-engineering-llm.md` references `../ARCHITECTURE.md` which was rescoped to Argus-only

`Acceptance Test:` `.claude/tests/test_cross_refs.py` — no broken relative links in docs/.

`Behavioral Test: none` — pure link fix; cross-refs test is structural coverage.

**File Scope:**
- `Allowed Reads:` docs/
- `Allowed Writes:` docs/design-docs/business-diagnosis.md, docs/design-docs/autonomous-service-agent.md, docs/design-docs/qa-scoring.md, docs/design-docs/shared/evidence-citing.md, docs/references/harness-engineering-llm.md, docs/exec-plans/completed/0002-doc-garden-2026-07-04.md
- `Requires:` none
- `Risk Tier:` B

## 4. Progress

- [x] M0: Regrade QUALITY_SCORE.md
- [ ] M1: Fix broken cross-references

## 5. Decision Log
*Written during execution.*

## 6. Surprises & Discoveries
*None yet.*

## 7. Awaiting Steering
*None.*

## 8. Outcomes & Retrospective
*Written at completion.*
