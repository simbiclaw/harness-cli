# M0 — Signal directory and state schema (9006)

*Verification by subagent V at commit 4676be7 (2026-07-30).*

### [plan-confirmed] 2026-07-30 — M0 adversarial verification passed

---

## Acceptance Test

**Test:** `.claude/tests/test_pev_signals.py`
**Result:** PASSED (2/2 tests)

- `test_signal_dir_exists` — PASSED (`.pev-signals/` exists with `.gitkeep`)
- `test_state_schema_validates` — PASSED (all required fields valid with correct types)

---

## Deterministic Sensors

### Structural tests (`uv run pytest .claude/tests/ -q`)
150 passed, **2 pre-existing failures** (NOT introduced by M0):

| Test | Failure | Root cause |
|---|---|---|
| `test_commit_messages.py` | `76c522a8`, `b962b730`, `fc8a713a` missing `Plan:` trailer | Commits predating M0 |
| `test_pev_agent_persistence.py` | pev-loop.md missing persistence sections | pev-loop.md content predates M0; M0 does not touch this file |

Both failures verified as pre-existing on parent commit `76c522a`.

### Linter (`ruff check .`)
8 errors — all in files outside M0's scope (`src/argus/types/compiler_schemas.py`, `tests/integration/test_pev_evolved_e2e.py`, `tests/test_compiler_schemas.py`). None touched by M0.

### Format checker (`ruff format --check .`)
3 files would be reformatted — all outside M0's scope. None touched by M0.

---

## Edge Case Analysis (Adversarial)

Three targeted probes not covered by the Acceptance Test:

### 1. ISO-8601 validation gap
**Probe:** `last_checkpoint_at` set to `"not-a-date"`, `"2026-13-01T00:00:00Z"`, `"yesterday"`, `""`
**Result:** All rejected by Python's `datetime.fromisoformat()`, but the test does NOT call `fromisoformat()` — it only checks the field is a non-empty string.
**Classification:** Mechanical — test quality gap. The spec says "ISO-8601 string" but the test does not enforce it. Current state.json value `2026-07-29T11:49:48Z` is valid.

### 2. Extra unvalidated fields (`arbiter_pid`, `tmux_session`)
**Probe:** `state.json` contains fields `arbiter_pid` and `tmux_session` which are NOT in `REQUIRED_STATE_FIELDS`.
**Result:** These pass through unvalidated. The test only checks that required fields exist and are valid.
**Classification:** No defect — schema intentionally allows forward-compatible extension fields. `None` values for both are valid.

### 3. Zero-padded milestone keys (`M00`, `M000`)
**Probe:** Regex `re.fullmatch(r"M\d+", key)` matches `M00` and `M000`.
**Result:** Valid per regex — zero-padded indices pass through.
**Classification:** Semantic — low severity. Convention assumes `M0`, `M1`, etc. Zero-padding could cause ambiguity in sort order but is not an implementation defect.

No edge case produced a concrete falsification of the implementation.

---

## Verdict

**Verdict: CONFIRMED**

M0 implementation is sound:
- Acceptance Test passes on the commit
- All files within M0's scope (`.pev-signals/.gitkeep`, `.pev-signals/state.json`, `.claude/tests/test_pev_signals.py`) are clean
- Pre-existing deterministic sensor failures are environmental (outside M0's scope, verified on parent commit)
- Adversarial edge cases found test quality gaps but no implementation defects

### Recommended follow-up (post-M0)
- Add ISO-8601 validation to `test_pev_signals.py` for `last_checkpoint_at` (low priority, test improvement)
- Document zero-padded milestone key convention if ambiguity arises (defer)
