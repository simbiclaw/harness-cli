### [plan-confirmed] 2026-07-30 — M6 adversarial verification passed
# M6 — Promotion arbiter (9006)

*Verification by subagent V against working tree (uncommitted M6 changes).*

---

## Acceptance Test

**Test:** `.claude/tests/test_promotion_arbiter.py` (8 tests)
**Result:** ALL 8 PASSED

| Class | Tests | Result |
|---|---|---|
| `TestViolationsDirAccessible` | 2 tests (dir exists, readable) | ALL PASSED |
| `TestPromotionInstructionsInPrompt` | 2 tests (references violations, mentions promotion) | ALL PASSED |
| `TestMechanicalPromotion` | 1 test (auto-execute described) | PASSED |
| `TestArchitecturalPromotion` | 2 tests (draft ExecPlan, requires human) | ALL PASSED |
| `TestViolationSchemaIntegration` | 1 test (write/read round trip) | PASSED |

## Deterministic Sensors

- **Structural tests:** 177 passed, 4 pre-existing failures — no regressions.
- **Linter:** 8 pre-existing errors — unchanged.
- **Format checker:** 3 pre-existing files — unchanged.

---

## Adversarial Edge Case Analysis

### Edge Case 1: Real violation records (mechanical + architectural)
**Probe:** Write one mechanical record (`documentation → structural test`) and one architectural record (`structural test → hook`), then generate prompt.
**Result:** Exit 0. Prompt contains both "auto-execute by creating the structural test" (mechanical) and "draft a pre-filled ExecPlan for human approval" (architectural). All assertions pass.
**Classification:** No defect.

### Edge Case 2: Single violation (insufficient data)
**Probe:** Write a violation record with `violation_count: 1` and `suggested_promotion: "insufficient data"`.
**Result:** Exit 0. Prompt correctly uses the threshold "When violation_count >= 2" — single violations are below threshold.
**Classification:** No defect. Threshold condition handled correctly in prompt text.

### Edge Case 3: Corrupted violation record JSON
**Probe:** Write garbage JSON to a violation record file.
**Result:** Exit 0. The script's prompt instructions are static (the heredoc is generated from fixed template text, not from reading violation records). The corruption has no effect on prompt generation.
**Classification:** No defect. The `--plan` and `--milestones` arguments drive prompt generation, not reading violation records. B is instructed to "monitor" the directory; record parsing is B's responsibility.

### Edge Case 4: Many violation records (10)
**Probe:** Create 10 violation records in the directory, then generate prompt.
**Result:** Exit 0. Prompt size is constant at 2914 chars / 61 lines regardless of how many records exist. The static instructions don't scale with violations.
**Classification:** No defect. The prompt tells B to monitor the directory and discover records independently.

---

## Implementation Review

The M6 additions are 9 lines in the prompt template (lines 194-202):

```
Monitor .pev-signals/violations/ for repeat violations of the same rule
across different ExecPlans. When violation_count >= 2, the rule is a
promotion candidate following the ladder:
    documentation → structural test → hook → CI gate → architecture

Mechanical promotions (documentation → structural test) are low-risk:
auto-execute by creating the structural test directly and committing.
Architectural promotions (hook → CI gate → architecture) are complex:
draft a pre-filled ExecPlan for human approval rather than auto-executing.
```

Key design point: The instructions are **static** — they do not parse or iterate over violation records during prompt generation. B is responsible for discovering and reading records at runtime from the directory. This is a valid design choice that keeps the script simple while pushing the exploration logic to the autonomous agent.

---

## Verdict

**Verdict: CONFIRMED**

M6 implementation passes all 8 Acceptance Tests. The promotion arbiter instructions are correctly embedded in the B prompt, distinguishing between mechanical (auto-execute) and architectural (draft+human) promotions. No regressions.
