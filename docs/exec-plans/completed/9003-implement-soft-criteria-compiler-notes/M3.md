# M3 — Corroborator classifier + residue declarer (9003)

### [plan-confirmed] — Four pure functions landed per contract

classify_corroborators (A3), declare_residue (A4), classify_gap (rev.4),
assign_escape_tier. Pure: stdlib + validator helpers only; no model client.

### [discovery] — I6 independent families needed explicit classification

The plan's tests pinned acoustic_measurement → independent, but the I6 weight
table (CLAUDE.md) defines the full independent family: acoustic measurement,
lexical/lookup/ordered-match (programmatic facets are model-free per D5).
B-verification round 1 found lexical_match/ordered_match/lookup/duration/
turn_count misclassified as correlated (1.0 → 0.4); closed.

### [discovery] — Shared signal_type normalization

Framework exclusion (D16) and classification now share ONE normalization:
strip → casefold → camelCase boundary split → separator collapse
(space/hyphen/underscore). Three adversarial rounds converged on it
(padding → separators → camelCase). Residuals: exotic tokens (dots, internal
tabs, merged-caps), marker-substring sweeps of undefined tokens
(turnaround/return → independent — documented latent).

### [plan-confirmed] — classify_gap: any gate-checkable coverage is never coverage

Rule order: values(phrases) → proxy(thresholds) → values(any checkable) →
calibration_surface_form(all model + depends_on) → perceiver(all model) →
coverage. The B-F split shape (checkable + model_based siblings) lands in
"values" with a truthful "bridge gap" rationale — never a false
"no compiled signals" claim.
