# M2 — Signal Decomposition + Evidence authoring (9003)

### [plan-confirmed] — Six pure functions landed per contract

decompose_dimension (A1), decompose_signals (A2+B-E), audit_gate_checkable
(B-F), assign_facets (D5/D9), compile_acoustic_framework (A2-ac),
compile_phrase_lexicon (A2-ph). Pure: stdlib re + validator helpers +
compiler_schemas only; no model client (I1 quarantine holds).

### [discovery] — Adversarial verification converged over 7 rounds

The ordered-relation detector's connective family (再/然后/后) was attacked
level by level: string-bool and defer-substring (B1/B2) → no-silent-drop and
RubricItem support (B3/B4/B5) → doubled connectives (BLOCK-1) → mirror
closures on first/second elements (F1/F2) → word-aware cuts (售后/后台) →
marker-position anchoring (B6). Each round closed with red tests; round 7
CONFIRMED. Residuals: 最后-as-marker asymmetry, connectives outside the
closed family (随后/旋即), English multi-step chains.

### [plan-confirmed] — decompose<->audit single source of truth

audit_gate_checkable precedence: observable markers → pass; split markers →
split; else model_only. decompose_signals stores audit_gate_checkable(s) as
each signal's audit_result, so the battery invariant holds by construction.

### [discovery] — Protected-word set for connective cuts

{售后, 后来, 后台, 随后, 最后, 然后} must never be split by marker anchoring,
cuts, or leading strips. Word-internal 后 (售后/后台) is the realistic trap
("先处理售后问题" must not yield (处理售, 问题)).
