# QUALITY_SCORE.md

Graded matrix of the current state of Argus (this repo). Rows are every domain × layer cell from `ARCHITECTURE.md § 3` that has implementation surface. Columns are five empirical dimensions. Every cell carries a grade from `A | B | C | D | F` plus a one-sentence justification linking to evidence.

This file does not describe how the system should be. It describes how it currently is. At bootstrap, the system is empty; every cell is `F` with the justification "no implementation yet — bootstrap state." This is correct, not pessimistic — the point of this file is to make the gap between intent (in `ARCHITECTURE.md`) and reality (in code) **visible and trackable**, not to render an opinion of how things ought to be.

The grading is regenerated by a grader subagent on a schedule. Do not hand-edit grades unless you have evidence in hand for each grade you change; hand-edits without evidence will be flagged by the doc-gardener and reverted.

## Dimensions

| Column | Definition | Source of evidence |
|---|---|---|
| **coverage** | Implementation completeness against the layer's responsibilities as named in `ARCHITECTURE.md § 5`. | Test inventory, code coverage report. |
| **boundary-respect** | Whether the layer's imports respect the layered model and dependency matrix. | `import-linter` (`.importlinter`) and `tools/lint/` outputs. |
| **boring-tech-adherence** | Whether the layer uses the choices in the boring-tech ledger or has drifted to alternatives. | Direct code inspection; explicit ADR references for every deviation. |
| **doc-freshness** | Whether the corresponding `product-specs/` and `design-docs/` files are current with the v6 spine. | Doc-gardener output; last-reviewed dates on specs. |
| **test-floor-met** | Whether every method has the verification-floor test (failing-before, passing-after). | Test inventory cross-referenced with `docs/conventions/verification-floor.md`. |

## Matrix

`Last graded: 2026-08-17` (doc-garden 9009, M0). `Next regrade: 2026-08-31`.

The matrix reflects the v6 rescope: Argus-only, five domains mapped onto five layers (`types → config → io → core → cli`). Non-Argus domains (AudioIntake, DocIngestion, ConvDistillation, Metis, Hermes) are not in this repo — see `docs/references/platform-architecture.md`. Knowledge Calibration and Expertise Library are dissolved as code domains (ADR-0003, ADR-0004); their responsibilities are in the INTENTS tree and the transformation layer. Since the 2026-07-31 baseline, the 9003 compiler core has landed (M0–M7 CONFIRMED) — the matrix below reflects that reality, not a date bump.

| Domain × Layer | coverage | boundary-respect | boring-tech-adherence | doc-freshness | test-floor-met |
|---|---|---|---|---|---|
| **types** | C (compiler_schemas.py real — 15 Pydantic models incl. AuthoredNode, RubricItem, CalibrationManifest, ResidueManifest, 284 LOC; § 5's FactCheckVerdict and INTENTS-read shapes still pending, 9002 in flight) | B (import-linter `argus layered architecture` KEPT — 1 kept, 0 broken; types imports stdlib+pydantic only, zero argus imports) | B (Pydantic per boring-tech ledger, declared in pyproject.toml with dep-vet-pydantic.md; boundary-parse lint enforces) | B (schemas specified in soft-criteria-authoring-spec-v4.html + patch-1/2; product-specs carry last-reviewed 2026-07-20, post-v6-spine) | B (test_compiler_schemas.py — 15 tests incl. parse and boundary-failure paths; M0 flipped CONFIRMED) |
| **config** | F (no impl) | C (import-linter enforced) | D (config keys named in ARCHITECTURE.md § 5; rubric-version and INTENTS-SHA pinning specified) | D (config keys documented in ARCHITECTURE.md) | F (no tests) |
| **providers+utils / io** | C (io/calibration_io.py implements the manifest channel — load_manifest yaml parse + apply_manifest_epoch pure re-anchor, 191 LOC; INTENTS tree epoch-stamped (011c94b) with 7 domains + _rubric shelf; the § 5 INTENTS-reader Provider itself is still pending, 9002) | B (import-linter KEPT; io imports argus.types + stdlib (+yaml) only — no model client in io, I1 quarantine holds) | D (filesystem+yaml per ledger, but PyYAML is imported by calibration_io.py and is absent from pyproject.toml — no dep-vet-pyyaml.md, new hygiene gap) | B (product-specs reviewed 2026-07-20; intents-semantic-layer.md, calibration.md two-axes amendment; compiler contract in soft-criteria-authoring-spec-v4.html + patches; EPOCH mechanism documented in INTENTS/EPOCH.yaml) | B (test_manifest_channel.py — 11 tests; test_intents_tree_wellformed.py parse+ownership+bindings; test_calibration_invariants.py bottom-up authority) |
| **core** | C (compiler core landed: core/compiler/{validator,signals,classify,agreement,bridge}.py — 2,260 LOC of pure logic; the § 5 two-stage score→adjust eval contract is still unimplemented, 9002 in flight) | B (import-linter KEPT; core/compiler imports only argus.types + stdlib (math/re/functools) — no io, no model client, I1 quarantine) | B (stdlib-only implementation, no external deps, no model client — matches the ledger's pure-core prescription) | B (fact-checking.md last-reviewed 2026-07-20 documents the two-stage contract + ADR-0001; compiler contract in soft-criteria-authoring-spec-v4.html + patch-1/2) | B (validator 66, signals 44, classify 25, agreement 12, bridge 18, pipeline 17 — 182 tests; M1–M5 flips CONFIRMED with 2–7 adversarial rounds each) |
| **cli** | D (Typer stub only — version command + no_args_is_help; production command surface pending 9004-skill-prototype-cli-production) | B (import-linter KEPT; cli imports typer + argus package root only) | B (Typer per ledger, declared in pyproject.toml with dep-vet-typer.md, used in cli/main.py) | D (no dedicated CLI product-spec; the 9004 plan in flight documents the surface) | D (test_cli_smoke.py exercises help/version and passes; no command logic yet to floor-test) |

Evidence for grade changes from the 2026-07-31 baseline:
- **boundary-respect B**: `uv run lint-imports` reports "argus layered architecture KEPT — Contracts: 1 kept, 0 broken"; code inspection confirms core/compiler and io import only types + stdlib, preserving the I1 model quarantine.
- **boring-tech B/D**: pydantic and typer are declared in pyproject.toml each with a dep-vet record (13 records cover all declared deps incl. anthropic, python-dotenv, rich); core is stdlib-only. New gap: PyYAML imported by `io/calibration_io.py` is not declared in pyproject.toml.
- **doc-freshness B**: all 25 product-spec/design-doc files carry `last-reviewed: 2026-07-20` (after the 2026-07-08 v6 spine review); the 9003 compiler contract lives in soft-criteria-authoring-spec-v4.html + patch-1/2; INTENTS/EPOCH.yaml documents the epoch mechanism (first real epoch 011c94b, 2026-08-13).
- **test-floor-met B**: 220 tests pass across the nine compiler/behavioral suites (validator 66, signals 44, classify 25, bridge 18, pipeline 17, schemas 15, befine 12, agreement 12, manifest 11); structural suite 193 passed / 2 failed — the 2 failures are the stale regrade dates, fixed by this regrade.
- **coverage C**: 2,735 LOC of real implementation in src/argus (types 284, io 191, core/compiler 2,260); the § 5 eval contract (score→adjust, FactCheckVerdict) and config pinning remain pending under 9002.

## Top five gaps (ordered by leverage)

1. **The § 5 evaluation core does not exist.** `score(facts, rubric)` → `adjust(raw, history)`, FactCheckVerdict, report/coaching emission, and config pinning (rubric_version, intents_sha) are all still pending. The 9003 compiler is real; the primary eval contract is not. **Closes**: `coverage` and `test-floor-met` for core, config, and parts of types. **Owner**: 9002-implement-argus-eval-pipeline (active).

2. **PyYAML is used but undeclared.** `io/calibration_io.py` imports yaml; pyproject.toml dependencies list no yaml/pyyaml and there is no dep-vet-pyyaml.md. A fresh environment build could break the io layer. **Closes**: `boring-tech-adherence` for io. **Owner**: declare the dependency + run dep-vetter.

3. **`forbidden-cross-domain-edges` lint checks the v1 10-domain matrix.** It must be updated for the 5-domain Argus matrix in `ARCHITECTURE.md § 3` (Revisit 2026-08-01 has passed). **Closes**: `boundary-respect` verification for the new domain set. **Owner**: future ExecPlan.

4. **No `utils-purity` lint exists.** Util modules could contain I/O or Provider access without detection (Revisit 2026-08-01 has passed). **Closes**: `boundary-respect` for providers+utils. **Owner**: future ExecPlan.

5. ~~**The doc-gardener subagent schedule is past due.**~~ → **Re-opened**: the 07-31 → 08-14 cycle slipped 3 days and tripped both structural gates; the cadence is tripwire-enforced (test_quality_score_regrade.py), not scheduler-enforced (9009 Surprises). **Closes**: `doc-freshness` auto-regrading. **Owner**: doc-gardener skill.

Gaps closed since bootstrap:
- ~~Lints that enforce the architecture do not exist~~ → 8 lints in `tools/lint/`, import-linter, CI gate — **closed** (0001-bootstrap M3).
- ~~Verification-floor convention does not exist~~ → `docs/conventions/verification-floor.md` exists — **closed** (0001-bootstrap M2).
- ~~Calibration's bottom-up-authority invariant has no test~~ → `test_calibration_invariants.py` exists — **closed** (0001-bootstrap M4).
- ~~Hermes action-tier types do not exist~~ → Hermes is not a domain in this repo — **no longer applicable** (v6 rescope).
- ~~No `src/argus/` implementation exists~~ → compiler core landed: types/compiler_schemas.py, io/calibration_io.py, core/compiler/{validator,signals,classify,agreement,bridge}.py, 220 tests passing — **closed** (9003 M0–M7).
- ~~Dep-vet records are incomplete~~ → all declared runtime deps (typer, pydantic, anthropic, python-dotenv, rich) have dep-vet records — **closed** (9003 era).

## Regrade policy

`Next regrade` is set 14 days out. The grader subagent re-runs the column-by-column inspection on schedule, diffs against this file, and posts a PR comment summarising movement.

Hand-edits to grades are permitted only when the editor includes evidence inline (a test name, a lint output, an ADR reference). Hand-edits without evidence are reverted by the doc-gardener; enforced by lint `quality-grade-evidence`.

`F` and `D` grades older than `Next regrade` without an active exec-plan in `docs/exec-plans/active/` whose Big Picture section names the failing cell are CI failures. (`F`-everywhere at bootstrap was exempted by `0001-bootstrap-the-spine.md`; the v6 upgrade `0000-upgrade-spine-to-v6.md` names the remaining cells; 9002 and 9004 name the current ones.)

## Forbidden phrases

Same set as the rest of the spine. Mechanical enforcement: `forbidden-phrases`.

`Last graded: 2026-08-17`
`Next regrade: 2026-08-31`
