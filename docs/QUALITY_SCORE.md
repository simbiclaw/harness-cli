# QUALITY_SCORE.md

Graded matrix of the current state of Argus (this repo). Rows are every domain × layer cell from `ARCHITECTURE.md § 3` that has implementation surface. Columns are five empirical dimensions. Every cell carries a grade from `A | B | C | D | F` plus a one-sentence justification linking to evidence.

This file does not describe how the system should be. It describes how it currently is. At bootstrap, the system is empty; every cell is `F` with the justification "no implementation yet — bootstrap state." This is correct, not pessimistic — the point of this file is to make the gap between intent (in `ARCHITECTURE.md`) and reality (in code) **visible and trackable**, not to render an opinion of how things ought to be.

The grading is regenerated by a grader subagent on a schedule. Do not hand-edit grades unless you have evidence in hand for each grade you change; hand-edits without evidence will be flagged by the doc-gardener and reverted.

## Dimensions

| Column | Definition | Source of evidence |
|---|---|---|
| **coverage** | Implementation completeness against the layer's responsibilities as named in `ARCHITECTURE.md § 5`. | Test inventory, code coverage report. |
| **boundary-respect** | Whether the layer's imports respect the layered model and dependency matrix. | `import-linter` (`.importlinter`) and `tools/lint/` outputs. |
| **boring-tech-adherence** | Whether the layer uses the choices in the boring-tech ledger or has drifted to alternatives. | Direct code inspection; explicit ADR references for every deviation. |
| **doc-freshness** | Whether the corresponding `product-specs/` and `design-docs/` files are current with the v6 spine. | Doc-gardener output; last-reviewed dates on specs. |
| **test-floor-met** | Whether every method has the verification-floor test (failing-before, passing-after). | Test inventory cross-referenced with `docs/conventions/verification-floor.md`. |

## Matrix

`Last graded: 2026-07-04` (v6 spine upgrade, M7). `Next regrade: 2026-08-03`.

The matrix reflects the v6 rescope: Argus-only, five domains mapped onto five layers (`types → config → io → core → cli`). Non-Argus domains (AudioIntake, DocIngestion, ConvDistillation, Metis, Hermes) are not in this repo — see `docs/references/platform-architecture.md`. Knowledge Calibration and Expertise Library are dissolved as code domains (ADR-0003, ADR-0004); their responsibilities are in the INTENTS tree and the transformation layer.

| Domain × Layer | coverage | boundary-respect | boring-tech-adherence | doc-freshness | test-floor-met |
|---|---|---|---|---|---|
| **types** | F (no impl) | C (import-linter enforced: types layer isolated) | D (Pydantic in pyproject.toml; boundary-parse lint enforces) | D (FactCheckVerdict shape specified in fact-checking.md; INTENTS node shapes in intents-semantic-layer.md) | F (no tests) |
| **config** | F (no impl) | C (import-linter enforced) | D (config keys named in ARCHITECTURE.md § 5; rubric-version and INTENTS-SHA pinning specified) | D (config keys documented in ARCHITECTURE.md) | F (no tests) |
| **providers+utils / io** | D (INTENTS tree landed with 14 files; _rubric/ shelf, L2 capsule, L3 case with anchored facts and history) | C (import-linter enforced) | C (filesystem+bash over RAG per boring-tech ledger; no drift) | C (intents-semantic-layer.md created; expertise-library.md reclassified; calibration.md two-axes amendment) | D (test_intents_tree_wellformed.py enforces parse+ownership+bindings) |
| **core** | F (no impl) | C (import-linter enforced) | D (two-stage score→adjust contract in fact-checking.md and ADR-0001) | C (fact-checking.md updated to two-stage contract; references ADR-0001) | F (no implementation tests) |
| **cli** | F (no impl) | C (import-linter enforced) | D (Typer in pyproject.toml) | D (cli smoke tests pass: help and version) | D (test_cli_smoke.py exercises help/version) |

Evidence for grade changes from bootstrap (all-F):
- **boundary-respect C**: `.importlinter` enforces `types→config→io→core→cli`; CI gate in `.github/workflows/harness.yml`; `uv run lint-imports` passes.
- **boring-tech-adherence D/C**: Pydantic and Typer are in `pyproject.toml` with dep-vet needed (pre-existing gap). Filesystem INTENTS reader specified but not implemented.
- **doc-freshness C/D**: v6 spine upgrade (0000-upgrade-spine-to-v6) edited expertise-library.md, fact-checking.md, calibration.md; created intents-semantic-layer.md; rewrote ARCHITECTURE.md; landed 4 ADRs; landed INTENTS tree. All specs carry `last-reviewed: 2026-07-04`.
- **test-floor-met D**: `test_intents_tree_wellformed.py` (parse, ownership, bindings) for INTENTS tree; `test_cli_smoke.py` for CLI surface.
- **coverage D**: INTENTS tree with worked domain (annual-report-submission) and `_rubric/` shelf (rules, 12 acoustic indicators, phrase lexicon) exists.

## Top five gaps (ordered by leverage)

1. **No `src/argus/` implementation exists.** All five domains are scaffolding only. The v6 spine defines the contract; the code doesn't exist yet. **Closes**: `coverage` and `test-floor-met` for all rows. **Owner**: first Argus exec-plan.

2. **Dep-vet records are incomplete.** `anthropic`, `pydantic`, `pytest-asyncio`, `python-dotenv`, `rich` are in `pyproject.toml` without dep-vet records. **Closes**: `boring-tech-adherence` for affected rows. **Owner**: run dep-vetter skill on each.

3. **`forbidden-cross-domain-edges` lint checks the v1 10-domain matrix.** It must be updated for the 5-domain Argus matrix in `ARCHITECTURE.md § 3`. **Closes**: `boundary-respect` verification for the new domain set. **Owner**: future ExecPlan (deferred by `0000-upgrade-spine-to-v6.md` Decision: no new lints in this plan).

4. **No `utils-purity` lint exists.** Util modules could contain I/O or Provider access without detection. **Closes**: `boundary-respect` for providers+utils. **Owner**: future ExecPlan.

5. ~~**The doc-gardener subagent schedule is past due.** `Next regrade: 2026-05-20` was missed; the doc-gardener needs to run to establish the new cadence.~~ **Resolved**: doc-gardener ran 2026-07-04 (`0002-doc-garden-2026-07-04.md`). Next regrade: 2026-07-18. **Closes**: `doc-freshness` auto-regrading. **Owner**: doc-gardener skill (`.claude/skills/doc-gardener/`).

Gaps closed since bootstrap:
- ~~Lints that enforce the architecture do not exist~~ → 8 lints in `tools/lint/`, import-linter, CI gate — **closed** (0001-bootstrap M3).
- ~~Verification-floor convention does not exist~~ → `docs/conventions/verification-floor.md` exists — **closed** (0001-bootstrap M2).
- ~~Calibration's bottom-up-authority invariant has no test~~ → `test_calibration_invariants.py` exists — **closed** (0001-bootstrap M4).
- ~~Hermes action-tier types do not exist~~ → Hermes is not a domain in this repo — **no longer applicable** (v6 rescope).

## Regrade policy

`Next regrade` is set 14 days out. The grader subagent re-runs the column-by-column inspection on schedule, diffs against this file, and posts a PR comment summarising movement.

Hand-edits to grades are permitted only when the editor includes evidence inline (a test name, a lint output, an ADR reference). Hand-edits without evidence are reverted by the doc-gardener; enforced by lint `quality-grade-evidence`.

`F` and `D` grades older than `Next regrade` without an active exec-plan in `docs/exec-plans/active/` whose Big Picture section names the failing cell are CI failures. (`F`-everywhere at bootstrap was exempted by `0001-bootstrap-the-spine.md`; the v6 upgrade `0000-upgrade-spine-to-v6.md` names the remaining cells.)

## Forbidden phrases

Same set as the rest of the spine. Mechanical enforcement: `forbidden-phrases`.

`Last graded: 2026-07-04`
`Next regrade: 2026-07-18`
