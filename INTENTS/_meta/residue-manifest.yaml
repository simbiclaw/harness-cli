# _meta/residue-manifest.yaml
# Residue manifest for compiled rubric nodes.
# Every compiler run that emits nodes produces or updates this manifest.
# Entries name what was left behind and why — within_dimension rows for
# lossy compilations, dimension_coverage_gap rows for unmapped items.
#
# Compiled by: 9003 soft-criteria compiler
# Generator: compile subcommand — argus compile <specific-rubric> <generic-skill> <align-md>
# Schema version: "1.0.0"
# Generated at: "2026-07-15T00:00:00Z"
# Sources:
#   - rubric_com_hotline.md
#   - 营销话术.md
#   - align.md

schema_version: "1.0.0"
generated_at: "2026-07-15T00:00:00Z"
compiler_epoch: "0000000000000000000000000000000000000000"
# Placeholder — replaced with actual git SHA on first compiler commit.

sources:
  - document: "rubric_com_hotline.md"
    scope: "Item 21 — 积极灵活营销"
    sha: null
  - document: "营销话术.md"
    scope: "18 standard marketing scripts"
    sha: null
  - document: "align.md"
    scope: "Item 21 → 业务引导 dimension"
    sha: null

calibration_manifest_epoch: null
# Not yet injected. Severity_map refs point to manifest/epoch-000 but are
# unresolved until the first calibration manifest is injected (M7).

residue_entries:
  - id: "RESIDUE-C21-WITHIN-DIM"
    source_item: "21"
    source_rule: "C21-001"
    entry_type: "within_dimension"
    dimension: "业务引导"
    description: >
      Item 21 compiles to the 业务引导 dimension with 5 signals and 3 corroborator
      classes. The compilation is lossy — 6 aspects of marketing quality are not
      captured by the signal decomposition (see residue_declared in the authored
      node). These gaps are expected for a calibration_surface_form criterion:
      the signal decomposition captures what is gate-checkable; tonal quality,
      timing judgment, creativity, customer receptivity, product aptness, and
      follow-through remain unmeasured until the calibration manifest covers
      their failure surfaces.
    loss_reason: "calibration_surface_form — model_based judgment cannot be fully decomposed into gate-checkable signals without calibration manifest coverage"
    gap_type: "calibration_surface_form"
    auto_final_permitted: false
    # AUTH-9: auto-final is forbidden until the calibration manifest covers
    # the failure surface for this criterion.
    affects_auto_final: true
    manifest_fix: >
      Populate the calibration manifest with Error Case Library fragments and
      Best Practice Cookbook entries covering the 6 gap dimensions (tonal
      quality, timing, creativity, receptivity, aptness, follow-through). When
      the manifest covers the failure surface, re-evaluate AUTH-9: if covered,
      set auto_final_permitted to true.
    unresolved_severity_refs:
      - "calibration://manifest/epoch-000/severity/c21"
